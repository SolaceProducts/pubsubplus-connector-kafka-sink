version: 3

project:
  locator: SolaceProducts_pubsubplus-connector-kafka-sink
  id: SolaceProducts_pubsubplus-connector-kafka-sink
  name: pubsubplus-connector-kafka-sink
  teams: []
  labels:
    - java

vendoredDependencies:
  forceRescans: false
  scanMethod: CLILicenseScan
  licenseScanPathFilters:
    exclude:
      - "./.git"
      - "./.github"

paths:
  exclude:
    - ./.git
    - ./.github

telemetry:
  scope: full
